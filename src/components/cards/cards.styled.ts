import styled from "@emotion/styled";

export const StyledCardsWrapper = styled.div({
  width: "80%",
  display: "flex",
  flexWrap: "wrap",
  gap: 8,
  justifyContent: "center",
  alignItems: "center",
});
